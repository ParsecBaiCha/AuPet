# 萌宠智伴 Vue3 前端项目 - 后端接口清单与数据结构需求分析报告

## 一、全局说明

### 1.1 API 基础配置
- **baseURL**: `/api`（所有请求前缀）
- **请求头**: `Content-Type: application/json`
- **认证方式**: `Authorization: Bearer {token}`（从 localStorage `pet-education-storage` 中读取 token）
- **响应拦截器**: `response.data`（axios 拦截器已处理，前端直接拿 `res` 就是后端返回的 JSON body）
- **401 处理**: 清除 localStorage 并跳转 `/login`

### 1.2 统一响应格式约定
前端代码中大量使用 `res?.success && Array.isArray(res.data)` 判断，说明后端统一响应格式应为：
```json
{
  "success": true,
  "data": { ... } 或 [ ... ],
  "message": "操作成功"
}
```
错误时：
```json
{
  "success": false,
  "message": "错误描述"
}
```

### 1.3 登录特殊说明
| 项目 | 说明 |
|------|------|
| 登录表单 | 只有一个 `username` 和 `password` 字段，不区分角色 |
| 学生登录 | username = 学号(student_no)，password = MD5加密（统一123456 = `e10adc3949ba59abbe56e057f20f883e`） |
| 教师登录 | username = 工号(employee_id)，password = 明文 |
| 管理员登录 | 如果 username 为 `admin`，后端直接返回 role='admin'（teachers 表没有 admin 记录） |
| 登录返回 | `{ token, user: { id, username, role, name, avatar? } }`，role 必须是 `'student'`/`'teacher'`/`'admin'` |
| 路由跳转 | 登录成功后 `router.push('/${role}')`，所以 role 值决定跳转路径 |

### 1.4 Store 状态结构

**app.ts (useAppStore)**:
```typescript
interface UserInfo {
  id: number
  username: string
  role: 'student' | 'teacher' | 'admin'
  avatar?: string
  name?: string
}
// 存储: { user: UserInfo | null, token: string | null }
```

**user.ts (useUserStore)** - 学生端专用状态：
```typescript
studentInfo = {
  name: string,        // 学生姓名
  avatar: string,      // 头像路径
  class: string,       // 班级
  email: string,       // 邮箱
  phone: string,       // 手机号
  bio: string,         // 个人简介
}
petInfo = {
  name: string,        // 宠物名字
  type: string,        // 宠物图片路径
  birthday: string,    // 生日
  gender: string,      // 性别
  bio: string,         // 宠物简介
}
petExp: number         // 当前经验值
petMaxExp: number      // 最大经验值
userPoints: number     // 用户积分
completedTasks: number  // 完成任务数
rank: number            // 班级排名
```

---

## 二、各页面详细分析

### 2.1 认证模块

#### 2.1.1 Login.vue
- **页面路径**: `src/views/auth/Login.vue`
- **API 调用**:
  - `authApi.login({ username, password })` — 点击登录按钮
- **期望返回数据**:
  ```json
  {
    "token": "jwt_token_string",
    "user": {
      "id": 1,
      "username": "20260001",
      "role": "student",
      "name": "张小明",
      "avatar": "/images/avatars/dz.jpg"
    }
  }
  ```
- **交互操作**:
  - 点击「登录」按钮 → 调用 `authApi.login()`
  - 登录成功 → `store.setToken(res.token)`, `store.setUser(res.user)`, `store.persist()`, 跳转 `/${role}`
  - 点击「注册」按钮 → 跳转 `/register`

#### 2.1.2 Register.vue
- **页面路径**: `src/views/auth/Register.vue`
- **API 调用**:
  - `authApi.register({ username, password, name, role })` — 点击注册按钮
- **请求参数**:
  ```json
  {
    "username": "string",
    "password": "string",
    "name": "string",
    "role": "student" | "teacher" | "admin"
  }
  ```
- **交互操作**:
  - 点击「立即注册」→ 调用 `authApi.register()`
  - 注册成功 → 跳转 `/login`

---

### 2.2 学生端模块

#### 2.2.1 Layout.vue (学生端布局)
- **页面路径**: `src/views/student/Layout.vue`
- **API 调用**: 无直接 API 调用
- **数据来源**: `useUserStore` (studentInfo, petInfo) + `useAppStore` (load)
- **onMounted**: `store.load()` 加载本地存储的登录状态
- **交互操作**:
  - 点击「退出」→ `store.logout()`, 跳转 `/login`
- **导航项**:
  - `/student` — 班级首页
  - `/student/my-pet` — 我的宠物
  - `/student/ai-companion` — 智能情感交流
  - `/student/growth-diary` — 成长日记
  - `/student/tasks` — 当日任务
  - `/student/settings` — 设置

#### 2.2.2 Home.vue (班级首页)
- **页面路径**: `src/views/student/Home.vue`
- **API 调用**: 当前使用 mock 数据（`classPets` 硬编码），API 定义中有 `studentApi.getDashboard()` 和 `studentApi.getClassMates()`
- **期望返回数据** (getDashboard):
  ```json
  {
    "success": true,
    "data": {
      "studentInfo": { "name", "avatar", "class" },
      "petInfo": { "name", "type" },
      "points": 1280,
      "completedTasks": 42,
      "rank": 5
    }
  }
  ```
- **期望返回数据** (getClassMates / 班级宠物列表):
  ```json
  {
    "success": true,
    "data": [
      {
        "id": 1,
        "studentName": "张小明",
        "petName": "球球",
        "petImage": "/images/pets/dog1.jpg",
        "points": 850,
        "level": "A",
        "progress": 85
      }
    ]
  }
  ```
- **页面用到的字段**:
  - `userStore.studentInfo.avatar`, `.name`, `.class`
  - `userStore.petInfo.type`, `.name`
  - `pet.studentName`, `pet.petName`, `pet.petImage`, `pet.points`, `pet.level`, `pet.progress`

#### 2.2.3 MyPet.vue (我的宠物)
- **页面路径**: `src/views/student/MyPet.vue`
- **API 调用**: 当前使用 mock 数据，API 定义中有 `studentApi.getMyPet()`, `studentApi.getShopItems()`, `studentApi.buyItem(itemId)`
- **期望返回数据** (getMyPet):
  ```json
  {
    "success": true,
    "data": {
      "name": "球球",
      "type": "/images/pets/dog1.jpg",
      "level": "A",
      "exp": 750,
      "maxExp": 1000,
      "health": 95,
      "hunger": 80,
      "happiness": 90,
      "adoptDate": "2026-01-15"
    }
  }
  ```
- **期望返回数据** (getShopItems):
  ```json
  {
    "success": true,
    "data": [
      { "id": 1, "name": "Pie", "image": "/images/foodimage/pie.jpg", "price": 50, "exp": 20 }
    ]
  }
  ```
- **交互操作**:
  - 点击食物项 → 弹出确认对话框
  - 点击「确认购买」→ 调用 `studentApi.buyItem(itemId)`（当前用 `userStore.spendPoints()` 本地处理）
  - 点击「和宠物聊天」→ 跳转 `/student/ai-companion`
- **页面用到的字段**: `userStore.studentInfo.*`, `userStore.petInfo.*`, `userStore.userPoints`, `userStore.petExp`, `userStore.petMaxExp`, `userStore.completedTasks`, `userStore.rank`

#### 2.2.4 AICompanion.vue (智能情感交流)
- **页面路径**: `src/views/student/AICompanion.vue`
- **API 调用**: 当前使用 mock 数据，API 定义中有 `studentApi.getChatHistory()`, `studentApi.sendChat(message)`
- **期望返回数据** (getChatHistory):
  ```json
  {
    "success": true,
    "data": [
      { "id": 1, "type": "pet", "content": "主人好！", "time": "09:00" },
      { "id": 2, "type": "user", "content": "你好！", "time": "09:01" }
    ]
  }
  ```
- **期望返回数据** (sendChat):
  ```json
  {
    "success": true,
    "data": { "reply": "主人说的对！我们要一起努力！" }
  }
  ```
- **交互操作**:
  - 输入消息点击「发送」→ 调用 `studentApi.sendChat(message)`（当前用本地 `petReply()` 模拟）
  - 点击快捷回复 → 自动发送
  - 点击「聊天记录」→ 展示历史消息
- **Message 结构**: `{ id, type: 'user'|'pet', content, time }`

#### 2.2.5 GrowthDiary.vue (成长日记)
- **页面路径**: `src/views/student/GrowthDiary.vue`
- **API 调用**: 当前使用 mock 数据，API 定义中有 `studentApi.getDiaries()`, `studentApi.createDiary(data)`, `studentApi.getEmotions()`, `studentApi.recordEmotion(data)`
- **期望返回数据** (getDiaries / 心情日记):
  ```json
  {
    "success": true,
    "data": [
      { "date": "2026-04-24", "mood": "/images/Mood_Diary/happy.jpg", "note": "今天完成了所有任务！" }
    ]
  }
  ```
- **期望返回数据** (getEmotions / 积分明细):
  ```json
  {
    "success": true,
    "data": [
      { "date": "2026-04-18", "points": 50 }
    ]
  }
  ```
- **交互操作**:
  - 选择心情图片 → 设置当日心情
  - 输入日记内容点击「添加」→ 调用 `studentApi.createDiary()` 或 `recordEmotion()`
  - 点击目标 → 切换完成状态
- **数据结构**:
  - MoodEntry: `{ date, mood, note }`
  - Goal: `{ id, title, completed, deadline }`
  - Achievement: `{ id, title, icon, description, unlocked }`
  - PointHistory: `{ date, points }`

#### 2.2.6 Tasks.vue (当日任务)
- **页面路径**: `src/views/student/Tasks.vue`
- **API 调用**: 当前使用 mock 数据，API 定义中有 `studentApi.getTasks()`, `studentApi.updateTask(id, data)`
- **期望返回数据** (getTasks):
  ```json
  {
    "success": true,
    "data": [
      {
        "id": 1,
        "title": "语文背诵",
        "description": "背诵课本第12课古诗并默写",
        "points": 20,
        "completed": false,
        "category": "语文",
        "deadline": "今日 18:00",
        "teacher": "王老师"
      }
    ]
  }
  ```
- **交互操作**:
  - 点击任务卡片 → 切换完成状态（当前本地处理，应调用 `studentApi.updateTask(id, { completed: true })`）
  - 点击分类筛选按钮 → 过滤任务列表
- **Task 结构**: `{ id, title, description, points, completed, category, deadline, teacher }`

#### 2.2.7 Settings.vue (设置)
- **页面路径**: `src/views/student/Settings.vue`
- **API 调用**: 当前使用 `useUserStore` 本地更新，未直接调用 API
- **期望 API**:
  - 更新学生信息: `PUT /api/student/profile` (待定义)
  - 修改密码: `PUT /api/student/password` (待定义)
  - 更新宠物信息: `PUT /api/student/pet` (待定义)
- **交互操作**:
  - 点击「保存信息」（个人信息）→ `userStore.updateStudentInfo(data)`
  - 点击「修改密码」→ 本地验证（应调用后端修改密码 API）
  - 点击「保存信息」（宠物信息）→ `userStore.updatePetInfo(data)`
- **页面字段**:
  - 学生: name, avatar, class, email, phone, bio
  - 宠物: name, type, birthday, gender, bio

---

### 2.3 教师端模块

#### 2.3.1 Layout.vue (教师端布局)
- **页面路径**: `src/views/teacher/Layout.vue`
- **API 调用**: 无直接 API 调用，从 `useAppStore` 获取用户信息
- **导航项**:
  - `/teacher` — 首页仪表盘
  - `/teacher/points` — 积分管理
  - `/teacher/class` — 班级管理
  - `/teacher/group-roles` — 群体智能角色管理
  - `/teacher/role-network` — 角色网络
  - `/teacher/predict` — 群体趋势预测
  - `/teacher/intervention` — 干预管理
  - `/teacher/forum` — 交流论坛

#### 2.3.2 Home.vue (教师首页仪表盘)
- **页面路径**: `src/views/teacher/Home.vue`
- **API 调用**: 当前使用 mock 数据，API 定义中有 `teacherApi.getDashboard()`
- **期望返回数据** (getDashboard):
  ```json
  {
    "success": true,
    "data": {
      "stats": {
        "studentCount": 128,
        "classCount": 3,
        "todayTask": 5,
        "noticeCount": 2
      },
      "abnormalStudents": [
        { "id": 6, "name": "赵小琳", "class": "一年级1班", "type": "内向安静", "date": "2026-04-25" }
      ],
      "weekTrend": [
        { "label": "一", "value": 78, "color": "#52c41a" }
      ],
      "recentActivities": [
        { "name": "李小红 - 课堂积极发言", "points": 5 }
      ],
      "classes": [
        { "id": 1, "name": "一年级1班", "studentCount": 20, "attendence": 96, "totalPoints": 15260 }
      ],
      "activities": [
        { "author": "李老师", "action": "发布了新公告", "avatar": "url", "time": "2小时前" }
      ],
      "schedule": [
        { "time": "08:00", "title": "语文课 · 一年级1班", "desc": "第1-2节" }
      ]
    }
  }
  ```

#### 2.3.3 PointManagement.vue (积分管理)
- **页面路径**: `src/views/teacher/PointManagement.vue`
- **API 调用**: 当前使用 mock 数据，API 定义中有 `teacherApi.getPoints()`, `teacherApi.awardPoints(data)`, `teacherApi.getPointRecords(studentId)`, `teacherApi.getPointRules()`, `teacherApi.createPointRule(data)`, `teacherApi.evaluateStudent(data)`
- **期望返回数据** (getPoints / 学生积分列表):
  ```json
  {
    "success": true,
    "data": [
      {
        "id": 1,
        "name": "张小明",
        "class": "一年级1班",
        "groupId": 1,
        "groupName": "星光组",
        "points": 850,
        "status": "active",
        "petLevel": "A级",
        "taskCompletionRate": 85,
        "moodIndex": 80,
        "personality": "活泼开朗，乐于助人"
      }
    ]
  }
  ```
- **期望返回数据** (getPointRules):
  ```json
  {
    "success": true,
    "data": [
      { "id": 1, "name": "按时完成作业", "points": 5 }
    ]
  }
  ```
- **期望返回数据** (getPointRecords / 学生积分记录):
  ```json
  {
    "success": true,
    "data": [
      { "name": "课堂积极回答", "points": 20, "time": "2026-04-22 10:30" }
    ]
  }
  ```
- **交互操作**:
  - 点击「积分」按钮 → 弹出调整积分对话框 → 调用 `teacherApi.awardPoints({ studentId, points, reason })`
  - 点击「详情」→ 弹出学生详情（含积分记录）→ 调用 `teacherApi.getPointRecords(studentId)`
  - 点击「发布任务」→ 弹出发布任务对话框 → 调用 `teacherApi.awardPoints()` 或新增任务API
  - 点击「小组加减分」→ 对小组内所有学生批量加减分
  - 点击「调组」→ 调整学生所在小组
  - 点击「性格分析」→ 弹出性格分析弹窗
  - 点击「评价」→ 弹出评价弹窗 → 调用 `teacherApi.evaluateStudent(data)`
- **awardPoints 请求参数**:
  ```json
  {
    "studentId": 1,
    "points": 10,
    "reason": "课堂积极发言"
  }
  ```
- **evaluateStudent 请求参数**:
  ```json
  {
    "studentId": 1,
    "score": 4,
    "tags": ["积极主动", "思维活跃"],
    "comment": "表现良好",
    "aiTraits": ["情绪稳定", "任务执行力强"]
  }
  ```

#### 2.3.4 ClassManagement.vue (班级管理)
- **页面路径**: `src/views/teacher/ClassManagement.vue`
- **API 调用**: 当前使用 mock 数据，API 定义中有 `teacherApi.getClasses()`, `teacherApi.createClass(data)`, `teacherApi.importClass(data)`, `teacherApi.getClassStudents(classId)`, `teacherApi.getStudentDetail(studentId)`
- **期望返回数据** (getClasses):
  ```json
  {
    "success": true,
    "data": [
      {
        "id": 1,
        "name": "一年级1班",
        "grade": "一年级",
        "teacher": "李老师",
        "studentCount": 20,
        "room": "101",
        "totalPoints": 15260,
        "avgPoints": 763,
        "psychologyStatus": "良好",
        "status": "normal"
      }
    ]
  }
  ```
- **期望返回数据** (getClassStudents):
  ```json
  {
    "success": true,
    "data": [
      {
        "name": "张小明",
        "gender": "male",
        "points": 850,
        "petLevel": "A",
        "taskRate": 85,
        "moodIndex": 4,
        "moodStatus": "良好"
      }
    ]
  }
  ```
- **交互操作**:
  - 点击「查看学生」→ 调用 `teacherApi.getClassStudents(classId)` → 弹出学生列表
  - 点击「编辑」→ 弹出编辑班级对话框 → 调用 `teacherApi.createClass()` 或更新API
  - 点击「删除」→ 调用删除API
  - 点击「导入班级」→ 弹出导入对话框 → 调用 `teacherApi.importClass(data)`
  - 点击「导出」→ 导出班级数据
- **createClass 请求参数**:
  ```json
  { "name": "一年级4班", "grade": "一年级", "teacher": "张老师", "room": "104" }
  ```

#### 2.3.5 GroupRoleManagement.vue (群体智能角色管理)
- **页面路径**: `src/views/teacher/GroupRoleManagement.vue`
- **API 调用**: 当前使用 mock 数据，API 定义中有 `teacherApi.getGroupRoles()`
- **期望返回数据** (getGroupRoles):
  ```json
  {
    "success": true,
    "data": [
      {
        "id": 1,
        "name": "张小明",
        "class": "一年级1班",
        "aiTraits": ["情绪稳定", "任务执行力强", "善于表达", "团队协作"],
        "chatRate": 78,
        "positiveRate": 72,
        "negativeRate": 5,
        "lastEvaluation": "良好",
        "lastEvalTime": "2026-04-20"
      }
    ]
  }
  ```
- **交互操作**:
  - 点击「评价」→ 弹出评价对话框 → 调用 `teacherApi.evaluateStudent(data)`
  - 筛选班级、搜索学生

#### 2.3.6 RoleNetwork.vue (角色网络图)
- **页面路径**: `src/views/teacher/RoleNetwork.vue`
- **API 调用**: 当前使用 mock 数据，API 定义中有 `teacherApi.getRoleNetwork()`
- **期望返回数据** (getRoleNetwork):
  ```json
  {
    "success": true,
    "data": {
      "nodes": [
        { "id": 1, "name": "张小明", "role": "leader", "group": 1, "traits": ["积极", "领导力"] }
      ],
      "links": [
        { "source": 1, "target": 2, "value": 5, "type": "friendship" }
      ]
    }
  }
  ```

#### 2.3.7 TrendPrediction.vue (群体趋势预测)
- **页面路径**: `src/views/teacher/TrendPrediction.vue`
- **API 调用**: 当前使用 mock 数据，API 定义中有 `teacherApi.getPredictions()`, `teacherApi.createPrediction(data)`, `teacherApi.getPredictionDetail(id)`
- **期望返回数据** (getPredictions):
  ```json
  {
    "success": true,
    "data": [
      {
        "id": 1,
        "title": "一年级1班心理趋势预测",
        "prediction": "整体趋势向好",
        "confidence": 85,
        "createTime": "2026-04-25"
      }
    ]
  }
  ```

#### 2.3.8 Intervention.vue (干预管理)
- **页面路径**: `src/views/teacher/Intervention.vue`
- **API 调用**: 当前使用 mock 数据，API 定义中有 `teacherApi.getInterventions()`, `teacherApi.getAbnormalDetail(id)`, `teacherApi.createIntervention(data)`
- **期望返回数据** (getInterventions / 异常学生列表):
  ```json
  {
    "success": true,
    "data": [
      {
        "id": 6,
        "name": "赵小琳",
        "class": "一年级1班",
        "abnormalType": "内向安静",
        "detectedDate": "2026-04-25",
        "severity": "轻度",
        "status": "ongoing",
        "interventionCount": 2
      }
    ]
  }
  ```
- **期望返回数据** (getAbnormalDetail):
  ```json
  {
    "success": true,
    "data": {
      "id": 6,
      "name": "赵小琳",
      "class": "一年级1班",
      "abnormalType": "内向安静",
      "detectedDate": "2026-04-25",
      "severity": "轻度",
      "status": "ongoing",
      "interventionCount": 2,
      "records": [
        { "title": "首次个别谈话", "date": "2026-04-25", "description": "...", "tags": ["个别谈话", "情绪疏导"] }
      ]
    }
  }
  ```
- **交互操作**:
  - 点击「新建干预」→ 弹出新建干预对话框 → 调用 `teacherApi.createIntervention(data)`
  - 点击「添加记录」→ 弹出添加记录对话框
  - 点击「详情」→ 弹出异常详情对话框
- **createIntervention 请求参数**:
  ```json
  {
    "studentId": 6,
    "abnormalType": "情绪低落",
    "severity": "轻度",
    "planId": 1,
    "goal": "帮助学生改善情绪状态"
  }
  ```

#### 2.3.9 Forum.vue (教师论坛)
- **页面路径**: `src/views/teacher/Forum.vue`
- **API 调用**: 当前使用 mock 数据，API 定义中有 `teacherApi.getForumPosts()`, `teacherApi.createForumPost(data)`, `teacherApi.getMyPosts()`, `teacherApi.getForumBoards()`
- **期望返回数据** (getForumPosts):
  ```json
  {
    "success": true,
    "data": [
      {
        "id": 1,
        "title": "如何提高学生的阅读理解能力",
        "content": "...",
        "author": "李老师",
        "avatar": "url",
        "forumId": 101,
        "forumName": "语文",
        "createTime": "2026-04-22 10:30",
        "views": 156,
        "replies": 15,
        "likes": 32,
        "top": true,
        "level": "资深教师"
      }
    ]
  }
  ```
- **期望返回数据** (getForumBoards):
  ```json
  {
    "success": true,
    "data": [
      { "id": 1, "name": "校园公告区", "count": 28 },
      { "id": 101, "name": "语文", "count": 45 }
    ]
  }
  ```
- **交互操作**:
  - 点击「发布帖子」→ 弹出发帖对话框 → 调用 `teacherApi.createForumPost(data)`
  - 点击帖子 → 弹出帖子详情对话框
  - 点击「我的帖子」→ 调用 `teacherApi.getMyPosts()`
- **createForumPost 请求参数**:
  ```json
  { "forumId": 101, "title": "标题", "content": "内容" }
  ```

---

### 2.4 管理员端模块

#### 2.4.1 Layout.vue (管理员布局)
- **页面路径**: `src/views/admin/Layout.vue`
- **API 调用**: 无直接 API 调用
- **导航项**:
  - `/admin` — 数据看板
  - `/admin/students` — 学生管理
  - `/admin/teachers` — 教师管理
  - `/admin/forum/daily` — 日常论坛
  - `/admin/forum/subject` — 学科论坛
  - `/admin/points` — 积分概览

#### 2.4.2 Home.vue (数据看板)
- **页面路径**: `src/views/admin/Home.vue`
- **API 调用**: 当前使用 mock 数据（ECharts 图表），API 定义中有 `adminApi.getDashboard()`
- **期望返回数据** (getDashboard):
  ```json
  {
    "success": true,
    "data": {
      "totalScore": 18960,
      "classScores": [
        { "class": "一年级1班", "score": 92 }
      ],
      "questionDistribution": [
        { "value": 28, "name": "日常问题" }
      ],
      "behaviorCompliance": 87,
      "classActivity": {
        "indicators": ["出勤率", "课堂参与度", "作业完成率", "纪律表现", "积分获取"],
        "values": [95, 88, 92, 78, 85]
      },
      "dailyTaskCompletion": [...],
      "onlineDistribution": [...],
      "pointsTrend": [...],
      "classRanking": [...]
    }
  }
  ```

#### 2.4.3 StudentManagement.vue (学生管理)
- **页面路径**: `src/views/admin/StudentManagement.vue`
- **API 调用**:
  - `adminApi.getStudents()` — onMounted
  - `adminApi.createStudent(data)` — 添加学生
  - `adminApi.updateStudent(id, data)` — 编辑学生
  - `adminApi.deleteStudent(id)` — 删除学生
- **期望返回数据** (getStudents):
  ```json
  {
    "success": true,
    "data": [
      {
        "sno": "20260001",
        "name": "张小明",
        "className": "一年级1班",
        "points": 850,
        "petLevel": "A级",
        "taskCompletionRate": 85,
        "moodIndex": 80,
        "status": 1
      }
    ]
  }
  ```
  **注意字段映射**（前端做了转换）:
  - `s.sno` → `id`（学生ID，即学号）
  - `s.name` → `name`
  - `s.className` → `class`
  - `s.points` → `points`
  - `s.petLevel` → `petLevel`
  - `s.taskCompletionRate` → `taskCompletionRate`
  - `s.moodIndex` → `moodIndex`（如果值 <= 5，前端会乘以 20 转为百分比）
  - `s.status` → `status`（1/'active' → 'active'）
- **交互操作**:
  - 点击「确认查询」→ 重新调用 `adminApi.getStudents()`
  - 点击「添加学生」→ 弹出对话框 → 调用 `adminApi.createStudent()`
  - 点击「编辑」→ 弹出对话框 → 调用 `adminApi.updateStudent()`
  - 点击「删除」→ 调用 `adminApi.deleteStudent()`
- **createStudent 请求参数**:
  ```json
  { "name": "张小明", "className": "一年级1班" }
  ```
- **updateStudent 请求参数**:
  ```json
  {
    "name": "张小明",
    "className": "一年级1班",
    "points": 900,
    "moodIndex": 80,
    "petLevel": "A级",
    "taskCompletionRate": 85
  }
  ```

#### 2.4.4 TeacherManagement.vue (教师管理)
- **页面路径**: `src/views/admin/TeacherManagement.vue`
- **API 调用**:
  - `adminApi.getTeachers()` — onMounted
  - `adminApi.createTeacher(data)` — 添加教师
  - `adminApi.updateTeacher(id, data)` — 编辑教师
  - `adminApi.deleteTeacher(id)` — 删除教师
- **期望返回数据** (getTeachers):
  ```json
  {
    "success": true,
    "data": [
      {
        "tno": "T001",
        "name": "王老师",
        "subject": "语文科研组",
        "teachingClass": "一年级1班,一年级2班",
        "qualification": "资深教师",
        "status": 1,
        "simulationCount": 15
      }
    ]
  }
  ```
  **注意字段映射**（前端做了转换）:
  - `t.tno` → `id`（教师ID，即工号）
  - `t.name` → `name`
  - `t.subject` → `subject`（所属科研组）
  - `t.teachingClass` → `teachingClass`（所教班级）
  - `t.qualification` → `qualification`（教师资历）
  - `t.status` → `status`
  - `t.simulationCount` → `simulationCount`
- **交互操作**:
  - 点击「确认查询」→ 重新调用 `adminApi.getTeachers()`
  - 点击「添加教师」→ 弹出对话框 → 调用 `adminApi.createTeacher()`
  - 点击「编辑」→ 弹出对话框 → 调用 `adminApi.updateTeacher()`
  - 点击「删除」→ 调用 `adminApi.deleteTeacher()`
- **createTeacher 请求参数**:
  ```json
  {
    "name": "王老师",
    "subject": "语文科研组",
    "teachingClass": "一年级1班,一年级2班",
    "qualification": "资深教师"
  }
  ```
- **updateTeacher 请求参数**:
  ```json
  {
    "name": "王老师",
    "subject": "语文科研组",
    "teachingClass": "一年级1班,一年级2班",
    "qualification": "资深教师",
    "simulationCount": 15
  }
  ```

#### 2.4.5 DailyForum.vue (日常论坛管理)
- **页面路径**: `src/views/admin/DailyForum.vue`
- **API 调用**:
  - `adminApi.getDailyForumPosts()` — onMounted
  - `adminApi.reviewPost(id, { status })` — 审核（通过/驳回）
  - `adminApi.toggleTop(id)` — 切换置顶
- **期望返回数据** (getDailyForumPosts):
  ```json
  {
    "success": true,
    "data": [
      {
        "id": 1,
        "title": "帖子标题",
        "content": "帖子内容",
        "authorName": "李老师",
        "status": "approved",
        "isTop": 1,
        "createTime": "2026-04-22T10:30:00"
      }
    ]
  }
  ```
  **注意字段映射**:
  - `p.authorName` → `author`
  - `p.isTop` → 前端转为 boolean（`=== 1 || === true`）
  - `p.createTime` → 前端做 `replace('T', ' ').slice(0, 16)` 格式化
  - `p.status` → `'pending'` / `'approved'` / `'rejected'`
- **交互操作**:
  - 点击「通过」→ 调用 `adminApi.reviewPost(id, { status: 'approved' })`
  - 点击「驳回」→ 调用 `adminApi.reviewPost(id, { status: 'rejected' })`
  - 点击置顶开关 → 调用 `adminApi.toggleTop(id)`
  - 点击「发布帖子」→ 弹出对话框（当前本地处理）
  - 点击「编辑」→ 弹出对话框（当前本地处理）

#### 2.4.6 SubjectForum.vue (学科论坛管理)
- **页面路径**: `src/views/admin/SubjectForum.vue`
- **API 调用**:
  - `adminApi.getSubjectForumPosts()` — onMounted
  - `adminApi.reviewPost(id, { status })` — 审核
  - `adminApi.toggleTop(id)` — 切换置顶
- **期望返回数据** (getSubjectForumPosts):
  ```json
  {
    "success": true,
    "data": [
      {
        "id": 1,
        "title": "帖子标题",
        "content": "帖子内容",
        "authorName": "李老师",
        "subject": "语文",
        "status": "approved",
        "isTop": 0,
        "createTime": "2026-04-22T10:30:00"
      }
    ]
  }
  ```
  - 比 DailyForum 多一个 `subject` 字段（学科分类）
- **交互操作**: 同 DailyForum，增加学科筛选

#### 2.4.7 PointOverview.vue (积分概览)
- **页面路径**: `src/views/admin/PointOverview.vue`
- **API 调用**: 当前使用 mock 数据，API 定义中有 `adminApi.getPointOverview()`, `adminApi.getPointRankings(params)`, `adminApi.getPointTrend()`
- **期望返回数据** (getPointOverview):
  ```json
  {
    "success": true,
    "data": {
      "totalRules": 8,
      "addRules": 5,
      "reduceRules": 3,
      "topClass": { "className": "二年级1班", "total": 18900, "avg": 859 },
      "topStudent": { "name": "赵丽", "class": "一年级1班", "points": 920 },
      "totalPoints": 18960,
      "studentCount": 128
    }
  }
  ```
- **期望返回数据** (getPointTrend):
  ```json
  {
    "success": true,
    "data": [
      { "day": "周一", "increase": 58, "decrease": 13 }
    ]
  }
  ```
- **期望返回数据** (getPointRankings / 班级积分统计):
  ```json
  {
    "success": true,
    "data": [
      { "className": "一年级1班", "total": 15260, "count": 20, "avg": 763 }
    ]
  }
  ```
- **页面用到的规则数据**:
  ```json
  [
    { "id": 1, "name": "课堂积极发言", "type": "add", "points": 5 },
    { "id": 5, "name": "迟到", "type": "reduce", "points": 2 }
  ]
  ```

---

## 三、后端需要实现的接口清单

### 3.1 认证模块

| 方法 | 路径 | 对应页面 | 返回数据字段要求 |
|------|------|----------|------------------|
| POST | `/api/auth/login` | Login.vue | `{ token: string, user: { id: number, username: string, role: 'student'\|'teacher'\|'admin', name: string, avatar?: string } }` |
| POST | `/api/auth/register` | Register.vue | `{ success: boolean, message: string }` |
| GET | `/api/auth/me` | 全局（刷新页面时获取当前用户） | `{ id, username, role, name, avatar? }` |

### 3.2 学生端模块

| 方法 | 路径 | 对应页面 | 返回数据字段要求 |
|------|------|----------|------------------|
| GET | `/api/student/dashboard` | student/Home.vue | `{ studentInfo: { name, avatar, class }, petInfo: { name, type }, points, completedTasks, rank }` |
| GET | `/api/student/mypet` | student/MyPet.vue | `{ name, type, level, exp, maxExp, health, hunger, happiness, adoptDate }` |
| GET | `/api/student/shop` | student/MyPet.vue | `[{ id, name, image, price, exp }]` |
| POST | `/api/student/shop/buy` | student/MyPet.vue | `{ success: boolean, message: string, data: { remainingPoints, newExp } }`，请求体: `{ itemId: number }` |
| GET | `/api/student/chat` | student/AICompanion.vue | `[{ id, type: 'user'\|'pet', content, time }]` |
| POST | `/api/student/chat` | student/AICompanion.vue | `{ reply: string }`，请求体: `{ message: string }` |
| GET | `/api/student/diaries` | student/GrowthDiary.vue | `[{ date, mood, note }]` |
| POST | `/api/student/diaries` | student/GrowthDiary.vue | `{ success: boolean }`，请求体: `{ date, mood, note }` |
| GET | `/api/student/emotions` | student/GrowthDiary.vue | `[{ date, points }]` |
| POST | `/api/student/emotions` | student/GrowthDiary.vue | `{ success: boolean }`，请求体: `{ date, mood, note }` |
| GET | `/api/student/tasks` | student/Tasks.vue | `[{ id, title, description, points, completed, category, deadline, teacher }]` |
| PUT | `/api/student/tasks/{id}` | student/Tasks.vue | `{ success: boolean }`，请求体: `{ completed: boolean }` |
| GET | `/api/student/classmates` | student/Home.vue | `[{ id, studentName, petName, petImage, points, level, progress }]` |
| GET | `/api/student/class-stats` | student/Home.vue | `{ totalStudents, avgPoints, ... }` |
| GET | `/api/student/pet-shop` | student/MyPet.vue | `[{ id, name, image, price, ... }]` |
| POST | `/api/student/pet-shop/buy` | student/MyPet.vue | `{ success: boolean }`，请求体: `{ petId: number }` |
| GET | `/api/student/points` | 学生全局 | `{ points: number, rank: number }` |
| PUT | `/api/student/profile` | student/Settings.vue | `{ success: boolean }`，请求体: `{ name, avatar, class, email, phone, bio }` |
| PUT | `/api/student/password` | student/Settings.vue | `{ success: boolean }`，请求体: `{ oldPassword, newPassword }` |
| PUT | `/api/student/pet` | student/Settings.vue | `{ success: boolean }`，请求体: `{ name, type, birthday, gender, bio }` |

### 3.3 教师端模块

| 方法 | 路径 | 对应页面 | 返回数据字段要求 |
|------|------|----------|------------------|
| GET | `/api/teacher/dashboard` | teacher/Home.vue | `{ stats: { studentCount, classCount, todayTask, noticeCount }, abnormalStudents: [{ id, name, class, type, date }], weekTrend: [{ label, value, color }], recentActivities: [{ name, points }], classes: [{ id, name, studentCount, attendence, totalPoints }], activities: [{ author, action, avatar, time }], schedule: [{ time, title, desc }] }` |
| GET | `/api/teacher/role-network` | teacher/RoleNetwork.vue | `{ nodes: [{ id, name, role, group, traits }], links: [{ source, target, value, type }] }` |
| GET | `/api/teacher/predictions` | teacher/TrendPrediction.vue | `[{ id, title, prediction, confidence, createTime }]` |
| POST | `/api/teacher/predictions` | teacher/TrendPrediction.vue | `{ success: boolean }`，请求体: `{ title, content, ... }` |
| GET | `/api/teacher/predictions/{id}` | teacher/TrendPrediction.vue | `{ id, title, prediction, confidence, createTime, details }` |
| GET | `/api/teacher/points` | teacher/PointManagement.vue | `[{ id, name, class, groupId, groupName, points, status, petLevel, taskCompletionRate, moodIndex, personality }]` |
| GET | `/api/teacher/points/records` | teacher/PointManagement.vue | `[{ name, points, time }]`，可选参数: `?studentId={id}` |
| POST | `/api/teacher/points/award` | teacher/PointManagement.vue | `{ success: boolean }`，请求体: `{ studentId, points, reason }` |
| GET | `/api/teacher/points/rules` | teacher/PointManagement.vue | `[{ id, name, points }]` |
| POST | `/api/teacher/points/rules` | teacher/PointManagement.vue | `{ success: boolean }`，请求体: `{ name, points }` |
| GET | `/api/teacher/classes` | teacher/ClassManagement.vue | `[{ id, name, grade, teacher, studentCount, room, totalPoints, avgPoints, psychologyStatus, status }]` |
| POST | `/api/teacher/classes` | teacher/ClassManagement.vue | `{ success: boolean }`，请求体: `{ name, grade, teacher, room }` |
| POST | `/api/teacher/classes/import` | teacher/ClassManagement.vue | `{ success: boolean }`，请求体: `{ classIds: number[] }` |
| GET | `/api/teacher/classes/{classId}/students` | teacher/ClassManagement.vue | `[{ name, gender, points, petLevel, taskRate, moodIndex, moodStatus }]` |
| GET | `/api/teacher/students/{studentId}` | teacher/PointManagement.vue, ClassManagement.vue | `{ id, name, class, points, petLevel, taskCompletionRate, moodIndex, personality, records: [{ name, points, time }] }` |
| GET | `/api/teacher/interventions` | teacher/Intervention.vue | `[{ id, name, class, abnormalType, detectedDate, severity, status, interventionCount }]` |
| GET | `/api/teacher/interventions/{id}` | teacher/Intervention.vue | `{ id, name, class, abnormalType, detectedDate, severity, status, interventionCount, records: [{ title, date, description, tags }] }` |
| POST | `/api/teacher/interventions` | teacher/Intervention.vue | `{ success: boolean }`，请求体: `{ studentId, abnormalType, severity, planId, goal }` |
| GET | `/api/teacher/group-roles` | teacher/GroupRoleManagement.vue | `[{ id, name, class, aiTraits: string[], chatRate, positiveRate, negativeRate, lastEvaluation, lastEvalTime }]` |
| POST | `/api/teacher/evaluations` | teacher/PointManagement.vue, GroupRoleManagement.vue | `{ success: boolean }`，请求体: `{ studentId, score, tags: string[], comment, aiTraits: string[] }` |
| GET | `/api/teacher/forum` | teacher/Forum.vue | `[{ id, title, content, author, avatar, forumId, forumName, createTime, views, replies, likes, top, level }]` |
| POST | `/api/teacher/forum` | teacher/Forum.vue | `{ success: boolean }`，请求体: `{ forumId, title, content }` |
| GET | `/api/teacher/forum/mine` | teacher/Forum.vue | `[{ id, title, content, author, forumId, forumName, createTime, views, replies, likes, top, level }]` |
| GET | `/api/teacher/forum/boards` | teacher/Forum.vue | `[{ id, name, count }]` |

### 3.4 管理员端模块

| 方法 | 路径 | 对应页面 | 返回数据字段要求 |
|------|------|----------|------------------|
| GET | `/api/admin/dashboard` | admin/Home.vue | `{ totalScore, classScores: [{ class, score }], questionDistribution: [{ value, name }], behaviorCompliance, classActivity: { indicators, values }, dailyTaskCompletion, onlineDistribution, pointsTrend, classRanking }` |
| GET | `/api/admin/students` | admin/StudentManagement.vue | `[{ sno, name, className, points, petLevel, taskCompletionRate, moodIndex, status }]`，可选参数: `?grade=&class=&petLevel=` |
| POST | `/api/admin/students` | admin/StudentManagement.vue | `{ success: boolean }`，请求体: `{ name, className }` |
| PUT | `/api/admin/students/{id}` | admin/StudentManagement.vue | `{ success: boolean }`，请求体: `{ name, className, points, moodIndex, petLevel, taskCompletionRate }` |
| DELETE | `/api/admin/students/{id}` | admin/StudentManagement.vue | `{ success: boolean }` |
| GET | `/api/admin/students/{id}` | admin/StudentManagement.vue | `{ sno, name, className, points, petLevel, taskCompletionRate, moodIndex, status }` |
| GET | `/api/admin/teachers` | admin/TeacherManagement.vue | `[{ tno, name, subject, teachingClass, qualification, status, simulationCount }]`，可选参数: `?qualification=&subject=` |
| POST | `/api/admin/teachers` | admin/TeacherManagement.vue | `{ success: boolean }`，请求体: `{ name, subject, teachingClass, qualification }` |
| PUT | `/api/admin/teachers/{id}` | admin/TeacherManagement.vue | `{ success: boolean }`，请求体: `{ name, subject, teachingClass, qualification, simulationCount }` |
| DELETE | `/api/admin/teachers/{id}` | admin/TeacherManagement.vue | `{ success: boolean }` |
| GET | `/api/admin/teachers/{id}` | admin/TeacherManagement.vue | `{ tno, name, subject, teachingClass, qualification, status, simulationCount }` |
| GET | `/api/admin/forum/daily` | admin/DailyForum.vue | `[{ id, title, content, authorName, status, isTop, createTime }]` |
| GET | `/api/admin/forum/subject` | admin/SubjectForum.vue | `[{ id, title, content, authorName, subject, status, isTop, createTime }]` |
| PUT | `/api/admin/forum/posts/{id}/review` | admin/DailyForum.vue, SubjectForum.vue | `{ success: boolean }`，请求体: `{ status: 'approved'\|'rejected' }` |
| PUT | `/api/admin/forum/posts/{id}/top` | admin/DailyForum.vue, SubjectForum.vue | `{ success: boolean }` |
| DELETE | `/api/admin/forum/posts/{id}` | admin/DailyForum.vue, SubjectForum.vue | `{ success: boolean }` |
| POST | `/api/admin/forum/announcement` | admin/DailyForum.vue | `{ success: boolean }`，请求体: `{ title, content, author }` |
| GET | `/api/admin/forum/boards` | 管理员论坛 | `[{ id, name, count }]` |
| POST | `/api/admin/forum/boards` | 管理员论坛 | `{ success: boolean }`，请求体: `{ name }` |
| PUT | `/api/admin/forum/boards/{id}` | 管理员论坛 | `{ success: boolean }`，请求体: `{ name }` |
| GET | `/api/admin/points/overview` | admin/PointOverview.vue | `{ totalRules, addRules, reduceRules, topClass: { className, total, avg }, topStudent: { name, class, points }, totalPoints, studentCount }` |
| GET | `/api/admin/points/rankings` | admin/PointOverview.vue | `[{ className, total, count, avg }]`，可选参数: `?grade=&class=` |
| GET | `/api/admin/points/trend` | admin/PointOverview.vue | `[{ day, increase, decrease }]` |

---

## 四、关键字段命名注意事项

### 4.1 学生字段映射（后端返回 → 前端使用）
| 后端字段 | 前端字段 | 说明 |
|----------|----------|------|
| `sno` | `id` | 学号，作为学生唯一标识 |
| `name` | `name` | 学生姓名 |
| `className` | `class` | 班级名称（如"一年级1班"） |
| `points` | `points` | 积分 |
| `petLevel` | `petLevel` | 宠物等级（S级/A级/B级/C级） |
| `taskCompletionRate` | `taskCompletionRate` | 任务完成率（百分比） |
| `moodIndex` | `moodIndex` | 心情指标（如果 <= 5 前端会 *20） |
| `status` | `status` | 1或'active' → 'active' |

### 4.2 教师字段映射（后端返回 → 前端使用）
| 后端字段 | 前端字段 | 说明 |
|----------|----------|------|
| `tno` | `id` | 工号，作为教师唯一标识 |
| `name` | `name` | 教师姓名 |
| `subject` | `subject` | 所属科研组（如"语文科研组"） |
| `teachingClass` | `teachingClass` | 所教班级（逗号分隔字符串） |
| `qualification` | `qualification` | 教师资历（资深教师/任课教师/实习教师） |
| `status` | `status` | 1或'active' → 'active' |
| `simulationCount` | `simulationCount` | 模拟次数 |

### 4.3 论坛帖子字段映射（后端返回 → 前端使用）
| 后端字段 | 前端字段 | 说明 |
|----------|----------|------|
| `id` | `id` | 帖子ID |
| `title` | `title` | 帖子标题 |
| `content` | `content` | 帖子内容 |
| `authorName` | `author` | 作者姓名 |
| `status` | `status` | 审核状态：'pending'/'approved'/'rejected' |
| `isTop` | `isTop` | 是否置顶：1或true |
| `createTime` | `createTime` | 创建时间（ISO格式，前端做格式化） |
| `subject` | `subject` | 学科分类（仅学科论坛） |

### 4.4 统一响应格式
所有接口返回统一格式：
```json
{
  "success": true,
  "data": { ... } | [ ... ],
  "message": "操作成功"
}
```
- 列表接口: `data` 为数组
- 详情接口: `data` 为对象
- 操作接口（POST/PUT/DELETE）: 可省略 `data`，只需 `success` 和 `message`

---

## 五、登录接口实现要点

### 5.1 登录逻辑流程
```
1. 前端发送 POST /api/auth/login { username, password }
2. 后端判断 username 类型:
   a. 如果 username === 'admin' → 返回 admin 角色
   b. 在 students 表中按 student_no 查找 → 密码比对 MD5(password)
   c. 在 teachers 表中按 employee_id 查找 → 密码比对明文
3. 返回 { token, user: { id, username, role, name, avatar? } }
```

### 5.2 密码处理
| 角色 | 密码存储方式 | 默认密码 |
|------|-------------|----------|
| 学生 | MD5 加密 | `e10adc3949ba59abbe56e057f20f883e` (即 123456) |
| 教师 | 明文 | 由管理员设置 |
| 管理员 | - | 特殊处理，username='admin' 即可登录 |

### 5.3 Token 生成
- 使用 JWT 或其他 token 方案
- Token 中应包含 `userId`、`role` 信息
- 前端会在每次请求时通过 `Authorization: Bearer {token}` 请求头携带

---

## 六、统计汇总

### 接口总数统计
| 模块 | 接口数 |
|------|--------|
| 认证模块 | 3 |
| 学生端 | 18 |
| 教师端 | 22 |
| 管理员端 | 22 |
| **合计** | **65** |

### 优先级分类
| 优先级 | 接口 | 说明 |
|--------|------|------|
| P0（必须先实现） | `/api/auth/login`, `/api/auth/register`, `/api/auth/me` | 登录是所有功能的基础 |
| P1（核心功能） | admin/students CRUD, admin/teachers CRUD, admin/forum/* CRUD | 管理员端已实际调用 API |
| P2（重要功能） | teacher/points, teacher/classes, teacher/interventions, teacher/forum | 教师端核心功能 |
| P3（增强功能） | student/dashboard, student/mypet, student/chat, student/tasks, student/diaries | 学生端目前用 mock 数据 |
| P4（数据可视化） | admin/dashboard, admin/points/*, teacher/role-network, teacher/predictions | 图表数据接口 |
