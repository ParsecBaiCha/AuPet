const fs=require('fs'),path=require('path');const out=path.join(__dirname,'integration');fs.mkdirSync(path.join(out,'public/online-programming'),{recursive:true});
const original='D:/GITHUB/aupet-new/2026081247-萌宠智伴-前端代码/frontend/src/views/student/AILearning.vue';let vue=fs.readFileSync(original,'utf8');if(!vue.includes("import OnlineProgrammingLesson"))vue=vue.replace("import DeepLearningLesson from './DeepLearningLesson.vue'","import DeepLearningLesson from './DeepLearningLesson.vue'\nimport OnlineProgrammingLesson from './OnlineProgrammingLesson.vue'");vue=vue.replace("if (activeTab.value === 'book') {","if (activeTab.value === 'book' && grade.value !== 'high_school') {");vue=vue.replace("if (tab === 'book') {","if (tab === 'book' && grade.value !== 'high_school') {");vue=vue.replace('<div v-if="activeTab === \'book\'" class="tab-content book-tab">','<div v-if="activeTab === \'book\' && grade === \'high_school\'" class="tab-content programming-tab">\n        <OnlineProgrammingLesson v-if="selectedCourse" :key="selectedCourse.id" :course-title="selectedCourse.title" :student-id="String(userStore.userInfo?.id || \'student\')" />\n        <div v-else class="empty-state"><p>请在左侧选择课程，开始在线编程练习。</p></div>\n      </div>\n      <div v-if="activeTab === \'book\' && grade !== \'high_school\'" class="tab-content book-tab">');vue=vue.replace(" :student-id=\"String(userStore.userInfo?.id || 'student')\"",'');fs.writeFileSync(path.join(out,'AILearning.vue'),vue);
fs.writeFileSync(path.join(out,'OnlineProgrammingLesson.vue'),`<script setup lang="ts">
import { computed } from 'vue'
const props = defineProps<{ courseTitle: string }>()
const source = computed(() => '/online-programming/index.html?course=' + encodeURIComponent(props.courseTitle))
</script>
<template><iframe class="programming-frame" :src="source" :title="courseTitle + '在线编程练习'" /></template>
<style scoped>.programming-frame{width:100%;height:calc(100vh - 230px);min-height:720px;border:0;display:block;background:white} @media(max-width:760px){.programming-frame{min-height:1100px}}</style>
`);
let html=fs.readFileSync('index.html','utf8');const begin=html.indexOf('<section class="course-header">');const end=html.indexOf('    </div></section></div>');const dialogBegin=html.indexOf('<dialog');const dialogEnd=html.indexOf('</dialog>')+9;html='<!doctype html><html lang="zh-CN"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>在线编程练习</title><link rel="stylesheet" href="style.css"></head><body><div class="grade-bar" hidden style="display:none"><div><strong></strong></div></div><main class="embedded-main">'+html.slice(begin,end)+'</main>'+html.slice(dialogBegin,dialogEnd)+'<script src="courses.js"></script><script src="python-runtime.js"></script><script src="app.js"></script><script src="tutor.js"></script></body></html>';fs.writeFileSync(path.join(out,'public/online-programming/index.html'),html);
let css=fs.readFileSync('style.css','utf8');css+='\nbody{background:#fff}main.embedded-main{margin:0;max-width:none;padding:24px}.course-header{margin-top:0}@media(max-width:650px){main.embedded-main{padding:16px}}\n';fs.writeFileSync(path.join(out,'public/online-programming/style.css'),css);
let app=fs.readFileSync('app.js','utf8');app=app.replace("function storageKey(){return courseId===",`function storageKey(){let student='student';try{student=JSON.parse(localStorage.getItem('pet-education-storage')||'{}').token||student;}catch{}return student+':'+(courseId===`).replace("'aupet-course-'+courseId+'-v1';}","'aupet-course-'+courseId+'-v1');}");app=app.replace("selectCourse('deep');","selectCourse(Object.keys(courseCatalog).find(id=>courseCatalog[id].title===new URLSearchParams(location.search).get('course'))||'deep');");fs.writeFileSync(path.join(out,'public/online-programming/app.js'),app);
let tutor=fs.readFileSync('tutor.js','utf8');tutor=`function authHeaders(){let token='';try{token=JSON.parse(localStorage.getItem('pet-education-storage')||'{}').token||'';}catch{}return {'Content-Type':'application/json',Authorization:'Bearer '+token};}\n`+tutor.replace("'/api/tutor'","'/api/student/ai/programming-tutor'").replace("headers:{'Content-Type':'application/json'}","headers:authHeaders()").replace("'/api/tutor/status',{signal:","'/api/student/ai/programming-tutor',{headers:authHeaders(),signal:");fs.writeFileSync(path.join(out,'public/online-programming/tutor.js'),tutor);
for(const file of ['courses.js','python-runtime.js','python-worker.js']){let text=fs.readFileSync(file,'utf8');if(file==='python-runtime.js')text=text.replace("'/python-worker.js'","'/online-programming/python-worker.js'");fs.writeFileSync(path.join(out,'public/online-programming',file),text);}
