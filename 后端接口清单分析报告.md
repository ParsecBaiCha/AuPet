# 萌宠智伴 - 后端接口清单分析报告

> 基于前端代码目录 `frontend/src` 的完整分析，整理出后端需要实现的所有接口及其数据结构需求。

---

## 一、全局说明

### 1.1 Axios 配置 (`src/api/index.ts`)

```typescript
baseURL: '/api'        // 所有请求前缀
timeout: 15000
// 请求拦截器：从 localStorage 的 'pet-education-storage' 中读取 token
// 响应拦截器：返回 response.data（即后端返回的 JSON body）
// 401 时清除 localStorage 并跳转 /login
```

**关键点：** 响应拦截器 `response.data` 意味着前端拿到的就是后端返回的 JSON body 本身，不存在 `{ code, data, message }` 这种外层包装。后端应直接返回业务数据。

### 1.2 统一响应格式

从前端代码分析，API 响应分两种模式：

- **直接返回数据：** 如 `authApi.login()` 返回 `{ token, user }`，`studentApi.getDashboard()` 返回仪表盘数据对象
- **列表返回：** 如 `adminApi.getStudents()` 返回 `{ success: true, data: [...] }`（见 StudentManagement.vue 中 `res?.success && Array.isArray(res.data)` 的判断）

**建议后端统一格式：**
```json
{
  "success": true,
  "data": <具体数据或数组>,
  "message": "操作成功"
}
```
但部分接口（如 login）前端直接取 `res.token` 和 `res.user`，需要特殊处理。

---

## 二、认证模块接口

### 2.1 POST `/api/auth/login`

- **对应页面：** `auth/Login.vue`
- **请求参数：**
  ```json
  {
    "username": "string",   // 学生填学号(student_no)，教师填工号(employee_id)，admin 填 "admin"
    "password": "string"    // 学生密码是 MD5 加密后传值；教师密码明文
  }
  ```
- **前端期望返回：**
  ```json
  {
    "token": "Bearer xxx",
    "user": {
      "id": 1,
      "username": "string",
      "role": "student | teacher | admin",
      "name": "string",
      "avatar": "string (可选)"
    }
  }
  ```
- **后端逻辑要求：**
  - 前端表单只有一个 `username` 字段，后端需判断是学号还是工号
  - 学生密码统一为 MD5(`123456`) = `e10adc3949ba59abbe56e057f20f883e`
  - 教师密码为明文
  - 数据库 teachers 表全是 `role='teacher'`，没有 admin；如果工号是 `admin` 或用户名是 `admin`，返回 `role: 'admin'`
  - 前端 Login.vue 中：`store.setToken(res.token)` 和 `store.setUser(res.user)`，然后 `router.push('/' + role)`

### 2.2 POST `/api/auth/register`

- **对应页面：** `auth/Register.vue`
- **请求参数：**
  ```json
  {
    "username": "string",
    "name": "string",
    "password": "string",
    "role": "student | teacher | admin"
  }
  ```
- **前端期望返回：** 成功即可（前端只判断是否成功，失败由拦截器弹 ElMessage）
- **后端逻辑：** 注册成功后跳转到登录页

### 2.3 GET `/api/auth/me`

- **对应页面：** 全局（App 初始化时可能调用）
- **前端期望返回：** 同 login 中的 `user` 对象
  ```json
  {
    "id": 1,
    "username": "string",
    "role": "student | teacher | admin",
    "name": "string",
    "avatar": "string (可选)"
  }
  ```

---

## 三、学生端接口 (`studentApi`)

> **注意：** 当前学生端页面大部分使用 mock 数据（Pinia store 中的硬编码数据），API 函数已定义但未在 onMounted 中调用。后端仍需实现这些接口，以便前端对接。

### 3.1 GET `/api/student/dashboard`

- **对应页面：** `student/Home.vue`（当前用 mock 数据）
- **期望返回：**
  ```json
  {
    "studentName": "张小明",
    "avatar": "/images/avatars/dz.jpg",
    "className": "一年级一班",
    "petName": "球球",
    "petImage": "/images/pets/dog1.jpg",
    "points": 1280,
    "rank": 5,
    "completedTasks": 42,
    "classPets": [
      {
        "id": 1,
        "studentName": "张小明",
        "petName": "球球",
        "petImage": "/images/pets/dog1.jpg",
        "points": 850,
        "level": "A",
        "progress": 85
      }
    ]
  }
  ```

### 3.2 GET `/api/student/mypet`

- **对应页面：** `student/MyPet.vue`（当前用 useUserStore mock 数据）
- **期望返回：**
  ```json
  {
    "pet": {
      "name": "球球",
      "type": "/images/pets/dog1.jpg",
      "level": "A",
      "exp": 750,
      "maxExp": 1000,
      "health": 95,
      "hunger": 80,
      "happiness": 90,
      "adoptDate": "2026-01-15"
    },
    "student": {
      "name": "张小明",
      "avatar": "/images/avatars/dz.jpg",
      "className": "一年级一班",
      "points": 1280,
      "completedTasks": 42,
      "rank": 5
    },
    "shopItems": [
      {
        "id": 1,
        "name": "Pie",
        "image": "/images/foodimage/pie.jpg",
        "price": 50,
        "exp": 20
      }
    ]
  }
  ```

### 3.3 GET `/api/student/shop`

- **对应页面：** `student/MyPet.vue`（商城部分）
- **期望返回：**
  ```json
  [
    {
      "id": 1,
      "name": "Pie",
      "image": "/images/foodimage/pie.jpg",
      "price": 50,
      "exp": 20
    }
  ]
  ```

### 3.4 POST `/api/student/shop/buy`

- **对应页面：** `student/MyPet.vue`（点击购买按钮）
- **请求参数：** `{ "itemId": 1 }`
- **期望返回：** `{ success: true, message: "购买成功" }`

### 3.5 GET `/api/student/chat`

- **对应页面：** `student/AICompanion.vue`
- **期望返回：**
  ```json
  [
    {
      "id": 1,
      "type": "pet | user",
      "content": "主人好！今天也要加油哦！",
      "time": "09:00"
    }
  ]
  ```

### 3.6 POST `/api/student/chat`

- **对应页面：** `student/AICompanion.vue`（发送消息）
- **请求参数：** `{ "message": "string" }`
- **期望返回：**
  ```json
  {
    "reply": "主人说的对！我们要一起努力！",
    "time": "09:02"
  }
  ```

### 3.7 GET `/api/student/diaries`

- **对应页面：** `student/GrowthDiary.vue`
- **期望返回：**
  ```json
  {
    "moodEntries": {
      "2026-04-24": { "date": "2026-04-24", "mood": "/images/Mood_Diary/happy.jpg", "note": "今天完成了所有任务！" }
    },
    "goals": [
      { "id": 1, "title": "每天背诵20个单词", "completed": true, "deadline": "2026-04-30" }
    ],
    "achievements": [
      { "id": 1, "title": "任务达人", "icon": "/images/Student_Icons/TaskMaster.jpg", "description": "完成10个任务", "unlocked": true }
    ],
    "pointHistory": [
      { "date": "2026-04-18", "points": 50 }
    ]
  }
  ```

### 3.8 POST `/api/student/diaries`

- **对应页面：** `student/GrowthDiary.vue`（添加心情笔记）
- **请求参数：**
  ```json
  {
    "date": "2026-04-24",
    "mood": "/images/Mood_Diary/happy.jpg",
    "note": "今天完成了所有任务！"
  }
  ```

### 3.9 GET `/api/student/emotions`

- **对应页面：** `student/GrowthDiary.vue`（心情记录部分）
- **期望返回：** 同 diaries 中的 moodEntries

### 3.10 POST `/api/student/emotions`

- **对应页面：** `student/GrowthDiary.vue`（设置心情）
- **请求参数：** `{ "date": "string", "mood": "string" }`

### 3.11 GET `/api/student/tasks`

- **对应页面：** `student/Tasks.vue`
- **期望返回：**
  ```json
  {
    "dailyTasks": [
      {
        "id": 1,
        "title": "语文背诵",
        "description": "背诵课本第12课古诗并默写",
        "points": 20,
        "completed": false,
        "category": "语文",
        "deadline": "今日 18:00",
        "teacher": "王老师"
      }
    ],
    "weeklyTasks": [
      {
        "id": 8,
        "title": "周记",
        "description": "完成本周一篇周记（不少于400字）",
        "points": 40,
        "completed": false,
        "category": "语文",
        "deadline": "周日 20:00",
        "teacher": "王老师"
      }
    ]
  }
  ```

### 3.12 PUT `/api/student/tasks/{id}`

- **对应页面：** `student/Tasks.vue`（点击切换任务完成状态）
- **请求参数：** `{ "completed": true }`

### 3.13 GET `/api/student/classmates`

- **对应页面：** `student/Home.vue`（班级宠物展示区）
- **期望返回：** 同 dashboard 中的 classPets 数组

### 3.14 GET `/api/student/class-stats`

- **对应页面：** `student/Home.vue`
- **期望返回：** 班级统计数据

### 3.15 GET `/api/student/pet-shop`

- **对应页面：** `student/MyPet.vue`（宠物商店）
- **期望返回：**
  ```json
  [
    { "id": 1, "name": "小狗", "image": "/images/pets/dog1.jpg", "price": 500 }
  ]
  ```

### 3.16 POST `/api/student/pet-shop/buy`

- **对应页面：** `student/MyPet.vue`（购买宠物）
- **请求参数：** `{ "petId": 1 }`

### 3.17 GET `/api/student/points`

- **对应页面：** `student/GrowthDiary.vue`（积分图表）
- **期望返回：**
  ```json
  [
    { "date": "2026-04-18", "points": 50 }
  ]
  ```

---

## 四、教师端接口 (`teacherApi`)

> **注意：** 教师端页面同样使用 mock 数据，API 函数已定义但未在 onMounted 中调用。

### 4.1 GET `/api/teacher/dashboard`

- **对应页面：** `teacher/Home.vue`
- **期望返回：**
  ```json
  {
    "stats": {
      "studentCount": 128,
      "classCount": 3,
      "todayTask": 5,
      "noticeCount": 2
    },
    "abnormalStudents": [
      { "id": 6, "name": "赵小琳", "class": "一年级1班", "type": "内向安静", "date": "2026-04-25" }
    ],
    "weekTrend": [
      { "label": "一", "value": 78, "color": "#52c41a" }
    ],
    "recentActivities": [
      { "name": "李小红 - 课堂积极发言", "points": 5 }
    ],
    "schedule": [
      { "time": "08:00", "title": "语文课 · 一年级1班", "desc": "第1-2节" }
    ],
    "classes": [
      { "id": 1, "name": "一年级1班", "studentCount": 20, "attendence": 96, "totalPoints": 15260 }
    ],
    "activities": [
      { "author": "李老师", "action": "发布了新公告", "avatar": "url", "time": "2小时前" }
    ]
  }
  ```

### 4.2 GET `/api/teacher/points`

- **对应页面：** `teacher/PointManagement.vue`
- **期望返回：** 学生列表
  ```json
  [
    {
      "id": 1,
      "name": "张小明",
      "class": "一年级1班",
      "groupId": 1,
      "groupName": "星光组",
      "points": 850,
      "status": "active",
      "petLevel": "A级",
      "taskCompletionRate": 85,
      "moodIndex": 80,
      "personality": "活泼开朗，乐于助人"
    }
  ]
  ```

### 4.3 GET `/api/teacher/points/records?studentId={studentId}`

- **对应页面：** `teacher/PointManagement.vue`（学生详情弹窗中的积分记录）
- **期望返回：**
  ```json
  [
    { "name": "课堂积极回答", "points": 20, "time": "2026-04-22 10:30" },
    { "name": "完成作业", "points": 10, "time": "2026-04-21 16:00" }
  ]
  ```

### 4.4 POST `/api/teacher/points/award`

- **对应页面：** `teacher/PointManagement.vue`（点击"积分"按钮，调整学生/小组积分）
- **请求参数：**
  ```json
  {
    "studentId": 1,           // 或 groupId 如果是小组加分
    "points": 10,             // 正数加分，负数扣分
    "reason": "课堂表现优秀"
  }
  ```
- **期望返回：** `{ success: true, message: "操作成功" }`

### 4.5 GET `/api/teacher/points/rules`

- **对应页面：** `teacher/PointManagement.vue`（发布任务弹窗中的积分规则列表）
- **期望返回：**
  ```json
  [
    { "id": 1, "name": "按时完成作业", "points": 5 },
    { "id": 2, "name": "课堂积极发言", "points": 2 }
  ]
  ```

### 4.6 POST `/api/teacher/points/rules`

- **对应页面：** `teacher/PointManagement.vue`（创建积分规则）
- **请求参数：** `{ "name": "string", "points": 5 }`

### 4.7 GET `/api/teacher/classes`

- **对应页面：** `teacher/ClassManagement.vue`
- **期望返回：**
  ```json
  [
    {
      "id": 1,
      "name": "一年级1班",
      "grade": "一年级",
      "teacher": "李老师",
      "studentCount": 20,
      "room": "101",
      "totalPoints": 15260,
      "avgPoints": 763,
      "psychologyStatus": "良好",
      "status": "normal"
    }
  ]
  ```

### 4.8 POST `/api/teacher/classes`

- **对应页面：** `teacher/ClassManagement.vue`（新增班级）
- **请求参数：**
  ```json
  {
    "id": 0,
    "name": "一年级1班",
    "grade": "一年级",
    "teacher": "李老师",
    "room": "101"
  }
  ```

### 4.9 POST `/api/teacher/classes/import`

- **对应页面：** `teacher/ClassManagement.vue`（导入班级）
- **请求参数：**
  ```json
  {
    "classIds": [1, 2, 3]
  }
  ```

### 4.10 GET `/api/teacher/classes/{classId}/students`

- **对应页面：** `teacher/ClassManagement.vue`（查看班级学生）
- **期望返回：**
  ```json
  [
    {
      "name": "张小明",
      "gender": "male",
      "points": 850,
      "petLevel": "S",
      "taskRate": 85,
      "moodIndex": 4,
      "moodStatus": "良好"
    }
  ]
  ```

### 4.11 GET `/api/teacher/students/{studentId}`

- **对应页面：** `teacher/PointManagement.vue`（学生详情）
- **期望返回：**
  ```json
  {
    "id": 1,
    "name": "张小明",
    "class": "一年级1班",
    "groupName": "星光组",
    "points": 850,
    "petLevel": "A级",
    "taskCompletionRate": 85,
    "moodIndex": 80,
    "personality": "活泼开朗，乐于助人",
    "records": [
      { "name": "课堂积极回答", "points": 20, "time": "2026-04-22 10:30" }
    ]
  }
  ```

### 4.12 GET `/api/teacher/interventions`

- **对应页面：** `teacher/Intervention.vue`
- **期望返回：**
  ```json
  [
    {
      "id": 6,
      "name": "赵小琳",
      "class": "一年级1班",
      "abnormalType": "内向安静",
      "detectedDate": "2026-04-25",
      "severity": "轻度",
      "status": "ongoing",
      "interventionCount": 2
    }
  ]
  ```

### 4.13 GET `/api/teacher/interventions/{id}`

- **对应页面：** `teacher/Intervention.vue`（查看异常详情）
- **期望返回：** 同上单个对象 + 详情描述 + 建议措施

### 4.14 POST `/api/teacher/interventions`

- **对应页面：** `teacher/Intervention.vue`（新建干预）
- **请求参数：**
  ```json
  {
    "studentId": "string",
    "abnormalType": "情绪低落",
    "severity": "轻度",
    "planId": "string",
    "goal": "干预目标描述"
  }
  ```

### 4.15 GET `/api/teacher/group-roles`

- **对应页面：** `teacher/GroupRoleManagement.vue`
- **期望返回：**
  ```json
  [
    {
      "id": 1,
      "name": "张小明",
      "class": "一年级1班",
      "lastEvaluation": "优秀",
      "lastEvalTime": "2026-04-20",
      "chatRate": 75,
      "positiveRate": 68,
      "negativeRate": 7,
      "aiTraits": ["情绪稳定", "任务执行力强"]
    }
  ]
  ```

### 4.16 POST `/api/teacher/evaluations`

- **对应页面：** `teacher/GroupRoleManagement.vue`（保存评价）/ `teacher/PointManagement.vue`（保存评价）
- **请求参数：**
  ```json
  {
    "studentId": 1,
    "score": 3,
    "tags": ["积极主动", "思维活跃"],
    "comment": "评语内容",
    "aiTraits": ["情绪稳定", "任务执行力强"],
    "chatAnalysis": {
      "activeRate": 75,
      "keywords": ["努力", "认真"]
    }
  }
  ```

### 4.17 GET `/api/teacher/role-network`

- **对应页面：** `teacher/RoleNetwork.vue`
- **期望返回：**
  ```json
  {
    "agents": [
      { "id": 1, "name": "智能体1", "x": 100, "y": 100, "color": "#8985cf", "role": "leader" }
    ],
    "links": [
      { "source": 1, "target": 2, "strength": 0.8 }
    ],
    "clusters": 3
  }
  ```

### 4.18 GET `/api/teacher/predictions`

- **对应页面：** `teacher/TrendPrediction.vue`
- **期望返回：** 预测结果列表
  ```json
  [
    {
      "id": 1,
      "title": "预测标题",
      "description": "预测描述",
      "createdAt": "2026-04-24"
    }
  ]
  ```

### 4.19 POST `/api/teacher/predictions`

- **对应页面：** `teacher/TrendPrediction.vue`（创建预测）
- **请求参数：** `{ "title": "string", "description": "string" }`

### 4.20 GET `/api/teacher/predictions/{id}`

- **对应页面：** `teacher/TrendPrediction.vue`（预测详情）
- **期望返回：** 预测详情对象

### 4.21 GET `/api/teacher/forum`

- **对应页面：** `teacher/Forum.vue`
- **期望返回：**
  ```json
  [
    {
      "id": 1,
      "title": "如何提高学生的阅读理解能力",
      "content": "内容...",
      "author": "李老师",
      "avatar": "url",
      "forumId": 101,
      "forumName": "语文",
      "createTime": "2026-04-22 10:30",
      "views": 156,
      "replies": 15,
      "likes": 32,
      "top": true,
      "level": "资深教师"
    }
  ]
  ```

### 4.22 POST `/api/teacher/forum`

- **对应页面：** `teacher/Forum.vue`（发布帖子）
- **请求参数：**
  ```json
  {
    "forumId": 1,
    "title": "帖子标题",
    "content": "帖子内容"
  }
  ```

### 4.23 GET `/api/teacher/forum/mine`

- **对应页面：** `teacher/Forum.vue`（我的帖子）
- **期望返回：** 同 forum 接口的帖子数组

### 4.24 GET `/api/teacher/forum/boards`

- **对应页面：** `teacher/Forum.vue`（论坛板块列表）
- **期望返回：**
  ```json
  [
    { "id": 1, "name": "校园公告区", "count": 28 },
    { "id": 101, "name": "语文", "count": 45 }
  ]
  ```

---

## 五、管理员端接口 (`adminApi`)

> **注意：** 管理员端是唯一实际调用了 API 的模块（StudentManagement、TeacherManagement、DailyForum、SubjectForum）。

### 5.1 GET `/api/admin/dashboard`

- **对应页面：** `admin/Home.vue`（当前使用 mock 数据）
- **期望返回：**
  ```json
  {
    "totalScore": 18960,
    "classScores": [
      { "class": "一年级1班", "score": 92 }
    ],
    "questionDistribution": {
      "daily": 28, "语文": 18, "数学": 15, "英语": 12, "科学": 10, "体育": 8, "美术": 7
    },
    "behaviorCompliance": 87,
    "classActivity": {
      "出勤率": 95, "课堂参与度": 88, "作业完成率": 92, "纪律表现": 78, "积分获取": 85
    },
    "dailyTaskCompletion": [...],
    "onlineDistribution": [...],
    "pointTrend": [...],
    "classRankings": [...]
  }
  ```

### 5.2 GET `/api/admin/students`

- **对应页面：** `admin/StudentManagement.vue` (**已实际调用**)
- **请求参数：** `?page=1&size=20&grade=&class=&petLevel=` (可选筛选)
- **期望返回：**
  ```json
  {
    "success": true,
    "data": [
      {
        "sno": "2026001",
        "name": "张小明",
        "className": "一年级1班",
        "points": 850,
        "petLevel": "A级",
        "taskCompletionRate": 85,
        "moodIndex": 80,
        "status": 1
      }
    ]
  }
  ```
- **字段映射说明（前端 StudentManagement.vue 中的映射逻辑）：**
  - `s.sno` → `id`
  - `s.name` → `name`
  - `s.className` → `class`
  - `s.points` → `points`
  - `s.petLevel` → `petLevel`（值为 "S级"/"A级"/"B级"/"C级"）
  - `s.taskCompletionRate` → `taskCompletionRate`
  - `s.moodIndex` → `moodIndex`（如果值<=5，前端会乘以20转换为百分比）
  - `s.status` → `status`（1 或 'active' 为正常）

### 5.3 POST `/api/admin/students`

- **对应页面：** `admin/StudentManagement.vue`（添加学生，**已实际调用**）
- **请求参数：**
  ```json
  {
    "name": "string",
    "className": "一年级1班"
  }
  ```

### 5.4 PUT `/api/admin/students/{id}`

- **对应页面：** `admin/StudentManagement.vue`（编辑学生，**已实际调用**）
- **请求参数：**
  ```json
  {
    "name": "string",
    "className": "一年级1班",
    "points": 850,
    "moodIndex": 80,
    "petLevel": "A级",
    "taskCompletionRate": 85
  }
  ```

### 5.5 DELETE `/api/admin/students/{id}`

- **对应页面：** `admin/StudentManagement.vue`（删除学生，**已实际调用**）

### 5.6 GET `/api/admin/students/{id}`

- **对应页面：** `admin/StudentManagement.vue`（学生详情，已定义但未在页面中调用）

### 5.7 GET `/api/admin/teachers`

- **对应页面：** `admin/TeacherManagement.vue` (**已实际调用**)
- **期望返回：**
  ```json
  {
    "success": true,
    "data": [
      {
        "tno": "T001",
        "name": "王老师",
        "subject": "数学科研组",
        "teachingClass": "一年级1班,一年级2班",
        "qualification": "资深教师",
        "status": 1,
        "simulationCount": 5
      }
    ]
  }
  ```
- **字段映射说明：**
  - `t.tno` → `id`
  - `t.name` → `name`
  - `t.subject` → `subject`
  - `t.teachingClass` → `teachingClass`
  - `t.qualification` → `qualification`（值为 "资深教师"/"任课教师"/"实习教师"）
  - `t.status` → `status`（1 或 'active' 为正常）
  - `t.simulationCount` → `simulationCount`

### 5.8 POST `/api/admin/teachers`

- **对应页面：** `admin/TeacherManagement.vue`（添加教师，**已实际调用**）
- **请求参数：**
  ```json
  {
    "name": "string",
    "subject": "数学科研组",
    "teachingClass": "一年级1班,一年级2班",
    "qualification": "任课教师"
  }
  ```

### 5.9 PUT `/api/admin/teachers/{id}`

- **对应页面：** `admin/TeacherManagement.vue`（编辑教师，**已实际调用**）
- **请求参数：**
  ```json
  {
    "name": "string",
    "subject": "数学科研组",
    "teachingClass": "一年级1班,一年级2班",
    "qualification": "资深教师",
    "simulationCount": 5
  }
  ```

### 5.10 DELETE `/api/admin/teachers/{id}`

- **对应页面：** `admin/TeacherManagement.vue`（删除教师，**已实际调用**）

### 5.11 GET `/api/admin/teachers/{id}`

- **对应页面：** `admin/TeacherManagement.vue`（教师详情，已定义但未调用）

### 5.12 GET `/api/admin/forum/daily`

- **对应页面：** `admin/DailyForum.vue` (**已实际调用**)
- **请求参数：** `?status=&page=&size=` (可选筛选)
- **期望返回：**
  ```json
  {
    "success": true,
    "data": [
      {
        "id": 1,
        "title": "帖子标题",
        "content": "帖子内容",
        "authorName": "张老师",
        "type": "daily",
        "status": "approved",
        "isTop": 1,
        "createTime": "2026-04-22T10:30:00"
      }
    ]
  }
  ```
- **字段映射说明（DailyForum.vue 和 SubjectForum.vue 中一致）：**
  - `p.id` → `id`
  - `p.title` → `title`
  - `p.content` → `content`
  - `p.authorName` → `author`
  - `p.subject` → `subject`（仅学科帖子）
  - `p.status` → `status`（值为 "pending"/"approved"/"rejected"）
  - `p.isTop` → `isTop`（1 或 true 表示置顶）
  - `p.createTime` → `createTime`（前端会执行 `.replace('T', ' ').slice(0, 16)` 格式化）

### 5.13 GET `/api/admin/forum/subject`

- **对应页面：** `admin/SubjectForum.vue` (**已实际调用**)
- **请求参数：** `?subject=&status=&page=&size=` (可选筛选)
- **期望返回：** 同上格式，但 `type` 为 "subject"，额外有 `subject` 字段

### 5.14 PUT `/api/admin/forum/posts/{id}/review`

- **对应页面：** `admin/DailyForum.vue` 和 `admin/SubjectForum.vue` (**已实际调用**)
- **请求参数：**
  ```json
  {
    "status": "approved | rejected"
  }
  ```

### 5.15 PUT `/api/admin/forum/posts/{id}/top`

- **对应页面：** `admin/DailyForum.vue` 和 `admin/SubjectForum.vue` (**已实际调用**)
- **请求参数：** 无（切换置顶状态）

### 5.16 DELETE `/api/admin/forum/posts/{id}`

- **对应页面：** `admin/DailyForum.vue`（已定义但页面中未实际调用 deletePost）

### 5.17 POST `/api/admin/forum/announcement`

- **对应页面：** `admin/DailyForum.vue`（发布公告，已定义但页面中未实际调用）
- **请求参数：**
  ```json
  {
    "title": "string",
    "content": "string"
  }
  ```

### 5.18 GET `/api/admin/forum/boards`

- **对应页面：** `admin/DailyForum.vue`（获取论坛板块，已定义但未实际调用）

### 5.19 POST `/api/admin/forum/boards`

- **对应页面：** 管理员论坛管理（创建板块）

### 5.20 PUT `/api/admin/forum/boards/{id}`

- **对应页面：** 管理员论坛管理（更新板块）

### 5.21 GET `/api/admin/points/overview`

- **对应页面：** `admin/PointOverview.vue`（当前使用 mock 数据）
- **期望返回：**
  ```json
  {
    "rules": [
      { "id": 1, "name": "课堂积极发言", "type": "add", "points": 5 },
      { "id": 5, "name": "迟到", "type": "reduce", "points": 2 }
    ],
    "classPoints": [
      { "className": "一年级1班", "total": 15260, "count": 20, "avg": 763 }
    ],
    "dailyPointsData": [
      { "day": "周一", "increase": 58, "decrease": 13 }
    ],
    "ruleDistribution": [
      { "value": 35, "name": "作业完成" },
      { "value": 25, "name": "课堂发言" }
    ]
  }
  ```

### 5.22 GET `/api/admin/points/rankings`

- **对应页面：** `admin/PointOverview.vue`（积分排名）
- **请求参数：** `?class=&grade=`

### 5.23 GET `/api/admin/points/trend`

- **对应页面：** `admin/PointOverview.vue`（积分趋势）
- **期望返回：**
  ```json
  {
    "trend": [...],
    "classRank": [...]
  }
  ```

---

## 六、后端接口清单汇总表

| 方法 | 路径 | 对应页面 | 返回数据字段要求 |
|------|------|----------|------------------|
| POST | `/api/auth/login` | auth/Login.vue | `{ token, user: { id, username, role, name, avatar? } }` |
| POST | `/api/auth/register` | auth/Register.vue | `{ success }` |
| GET | `/api/auth/me` | 全局 | `{ id, username, role, name, avatar? }` |
| GET | `/api/student/dashboard` | student/Home.vue | `{ studentName, avatar, className, petName, petImage, points, rank, completedTasks, classPets[] }` |
| GET | `/api/student/mypet` | student/MyPet.vue | `{ pet: { name, type, level, exp, maxExp, health, hunger, happiness, adoptDate }, student: { name, avatar, className, points, completedTasks, rank }, shopItems[] }` |
| GET | `/api/student/shop` | student/MyPet.vue | `[{ id, name, image, price, exp }]` |
| POST | `/api/student/shop/buy` | student/MyPet.vue | `{ success, message }` |
| GET | `/api/student/chat` | student/AICompanion.vue | `[{ id, type, content, time }]` |
| POST | `/api/student/chat` | student/AICompanion.vue | `{ reply, time }` |
| GET | `/api/student/diaries` | student/GrowthDiary.vue | `{ moodEntries, goals[], achievements[], pointHistory[] }` |
| POST | `/api/student/diaries` | student/GrowthDiary.vue | `{ success }` |
| GET | `/api/student/emotions` | student/GrowthDiary.vue | `{ moodEntries }` |
| POST | `/api/student/emotions` | student/GrowthDiary.vue | `{ success }` |
| GET | `/api/student/tasks` | student/Tasks.vue | `{ dailyTasks[], weeklyTasks[] }` |
| PUT | `/api/student/tasks/{id}` | student/Tasks.vue | `{ success }` |
| GET | `/api/student/classmates` | student/Home.vue | `[{ id, studentName, petName, petImage, points, level, progress }]` |
| GET | `/api/student/class-stats` | student/Home.vue | 班级统计数据 |
| GET | `/api/student/pet-shop` | student/MyPet.vue | `[{ id, name, image, price }]` |
| POST | `/api/student/pet-shop/buy` | student/MyPet.vue | `{ success }` |
| GET | `/api/student/points` | student/GrowthDiary.vue | `[{ date, points }]` |
| GET | `/api/teacher/dashboard` | teacher/Home.vue | `{ stats, abnormalStudents[], weekTrend[], recentActivities[], schedule[], classes[], activities[] }` |
| GET | `/api/teacher/points` | teacher/PointManagement.vue | `[{ id, name, class, groupId, groupName, points, status, petLevel, taskCompletionRate, moodIndex, personality }]` |
| GET | `/api/teacher/points/records` | teacher/PointManagement.vue | `[{ name, points, time }]` |
| POST | `/api/teacher/points/award` | teacher/PointManagement.vue | `{ success }` |
| GET | `/api/teacher/points/rules` | teacher/PointManagement.vue | `[{ id, name, points }]` |
| POST | `/api/teacher/points/rules` | teacher/PointManagement.vue | `{ success }` |
| GET | `/api/teacher/classes` | teacher/ClassManagement.vue | `[{ id, name, grade, teacher, studentCount, room, totalPoints, avgPoints, psychologyStatus, status }]` |
| POST | `/api/teacher/classes` | teacher/ClassManagement.vue | `{ success }` |
| POST | `/api/teacher/classes/import` | teacher/ClassManagement.vue | `{ success }` |
| GET | `/api/teacher/classes/{classId}/students` | teacher/ClassManagement.vue | `[{ name, gender, points, petLevel, taskRate, moodIndex, moodStatus }]` |
| GET | `/api/teacher/students/{studentId}` | teacher/PointManagement.vue | `{ id, name, class, groupName, points, petLevel, taskCompletionRate, moodIndex, personality, records[] }` |
| GET | `/api/teacher/interventions` | teacher/Intervention.vue | `[{ id, name, class, abnormalType, detectedDate, severity, status, interventionCount }]` |
| GET | `/api/teacher/interventions/{id}` | teacher/Intervention.vue | `{ id, name, class, abnormalType, detectedDate, severity, status, interventionCount, description, suggestions[] }` |
| POST | `/api/teacher/interventions` | teacher/Intervention.vue | `{ success }` |
| GET | `/api/teacher/group-roles` | teacher/GroupRoleManagement.vue | `[{ id, name, class, lastEvaluation, lastEvalTime, chatRate, positiveRate, negativeRate, aiTraits[] }]` |
| POST | `/api/teacher/evaluations` | teacher/GroupRoleManagement.vue | `{ success }` |
| GET | `/api/teacher/role-network` | teacher/RoleNetwork.vue | `{ agents[], links[], clusters }` |
| GET | `/api/teacher/predictions` | teacher/TrendPrediction.vue | `[{ id, title, description, createdAt }]` |
| POST | `/api/teacher/predictions` | teacher/TrendPrediction.vue | `{ success }` |
| GET | `/api/teacher/predictions/{id}` | teacher/TrendPrediction.vue | `{ id, title, description, createdAt }` |
| GET | `/api/teacher/forum` | teacher/Forum.vue | `[{ id, title, content, author, avatar, forumId, forumName, createTime, views, replies, likes, top, level }]` |
| POST | `/api/teacher/forum` | teacher/Forum.vue | `{ success }` |
| GET | `/api/teacher/forum/mine` | teacher/Forum.vue | 同 forum 接口格式 |
| GET | `/api/teacher/forum/boards` | teacher/Forum.vue | `[{ id, name, count }]` |
| GET | `/api/admin/dashboard` | admin/Home.vue | `{ totalScore, classScores[], questionDistribution, behaviorCompliance, classActivity, dailyTaskCompletion[], onlineDistribution[], pointTrend[], classRankings[] }` |
| GET | `/api/admin/students` | admin/StudentManagement.vue | `{ success, data: [{ sno, name, className, points, petLevel, taskCompletionRate, moodIndex, status }] }` |
| POST | `/api/admin/students` | admin/StudentManagement.vue | `{ success }` |
| PUT | `/api/admin/students/{id}` | admin/StudentManagement.vue | `{ success }` |
| DELETE | `/api/admin/students/{id}` | admin/StudentManagement.vue | `{ success }` |
| GET | `/api/admin/students/{id}` | admin/StudentManagement.vue | `{ sno, name, className, points, petLevel, taskCompletionRate, moodIndex, status }` |
| GET | `/api/admin/teachers` | admin/TeacherManagement.vue | `{ success, data: [{ tno, name, subject, teachingClass, qualification, status, simulationCount }] }` |
| POST | `/api/admin/teachers` | admin/TeacherManagement.vue | `{ success }` |
| PUT | `/api/admin/teachers/{id}` | admin/TeacherManagement.vue | `{ success }` |
| DELETE | `/api/admin/teachers/{id}` | admin/TeacherManagement.vue | `{ success }` |
| GET | `/api/admin/teachers/{id}` | admin/TeacherManagement.vue | `{ tno, name, subject, teachingClass, qualification, status, simulationCount }` |
| GET | `/api/admin/forum/daily` | admin/DailyForum.vue | `{ success, data: [{ id, title, content, authorName, type, status, isTop, createTime }] }` |
| GET | `/api/admin/forum/subject` | admin/SubjectForum.vue | `{ success, data: [{ id, title, content, authorName, subject, type, status, isTop, createTime }] }` |
| PUT | `/api/admin/forum/posts/{id}/review` | admin/DailyForum.vue, admin/SubjectForum.vue | `{ success }` |
| PUT | `/api/admin/forum/posts/{id}/top` | admin/DailyForum.vue, admin/SubjectForum.vue | `{ success }` |
| DELETE | `/api/admin/forum/posts/{id}` | admin/DailyForum.vue | `{ success }` |
| POST | `/api/admin/forum/announcement` | admin/DailyForum.vue | `{ success }` |
| GET | `/api/admin/forum/boards` | admin/DailyForum.vue | `[{ id, name }]` |
| POST | `/api/admin/forum/boards` | 管理员 | `{ success }` |
| PUT | `/api/admin/forum/boards/{id}` | 管理员 | `{ success }` |
| GET | `/api/admin/points/overview` | admin/PointOverview.vue | `{ rules[], classPoints[], dailyPointsData[], ruleDistribution[] }` |
| GET | `/api/admin/points/rankings` | admin/PointOverview.vue | `[{ name, class, points }]` |
| GET | `/api/admin/points/trend` | admin/PointOverview.vue | `{ trend[], classRank[] }` |

---

## 七、特别注意事项

### 7.1 登录接口特殊处理

```
POST /api/auth/login
```

**前端 Login.vue 核心逻辑：**
```typescript
const res: any = await authApi.login(form)
store.setToken(res.token)      // 直接取 res.token
store.setUser(res.user)         // 直接取 res.user
const role = res.user?.role || 'student'
router.push(`/${role}`)         // 根据 role 跳转
```

**后端必须返回的格式（不能有外层包装）：**
```json
{
  "token": "Bearer xxx",
  "user": {
    "id": 1,
    "username": "string",
    "role": "student",
    "name": "张小明",
    "avatar": "/images/avatars/dz.jpg"
  }
}
```

### 7.2 用户身份判断逻辑

前端登录表单只有一个 `username` 字段，后端需根据值判断身份：

| 输入 username | 实际身份 | 数据库查询 | 密码验证 |
|---------------|----------|------------|----------|
| 学号 (如 2026001) | 学生 | students 表 WHERE student_no = ? | MD5(明文密码) == 数据库 password |
| 工号 (如 T001) | 教师 | teachers 表 WHERE employee_id = ? | 明文密码 == 数据库 password |
| "admin" | 管理员 | 特殊处理 | 固定密码或特殊验证 |

### 7.3 学生密码 MD5

学生密码统一为 `123456`，数据库存储的应为 MD5 值：`e10adc3949ba59abbe56e057f20f883e`

前端登录时直接传输密码字段（明文或 MD5 取决于前端实现），需确认前端是否做 MD5。从 Login.vue 代码看，前端直接传 `form.password`（明文），因此：
- 如果学生输入 `123456`，后端需将其 MD5 后与数据库比对
- 或者数据库直接存明文 `123456`

### 7.4 Admin 角色处理

数据库 `teachers` 表全是 `role='teacher'`，没有 admin 记录。建议后端：

```python
if username == 'admin':
    # 特殊处理 admin 登录
    return { token: 'xxx', user: { id: 0, username: 'admin', role: 'admin', name: '管理员' } }
```

### 7.5 管理员列表接口的响应格式

管理员端四个管理页面（StudentManagement、TeacherManagement、DailyForum、SubjectForum）在 fetchXxx 函数中统一使用以下判断：

```typescript
const res = await adminApi.getStudents()
if (res?.success && Array.isArray(res.data)) {
    // 处理数据
}
```

**后端必须返回：**
```json
{
  "success": true,
  "data": [ ... ]
}
```

### 7.6 createTime 格式处理

论坛帖子列表中，前端对 `createTime` 做如下处理：
```typescript
createTime: p.createTime ? p.createTime.replace('T', ' ').slice(0, 16) : ''
```

后端返回的 `createTime` 可以是 ISO 格式（如 `2026-04-22T10:30:00`），前端会自动格式化为 `2026-04-22 10:30`。

### 7.7 isTop 字段处理

前端判断逻辑：
```typescript
isTop: p.isTop === 1 || p.isTop === true
```

后端返回 `0/1` 或 `false/true` 均可。

### 7.8 status 字段处理

学生/教师列表中：
```typescript
status: s.status === 1 || s.status === 'active' ? 'active' : 'inactive'
```

后端返回数字 `1` 或字符串 `'active'` 均可。

### 7.9 moodIndex 字段处理

学生列表中：
```typescript
moodIndex: typeof s.moodIndex === 'number' && s.moodIndex <= 5 ? s.moodIndex * 20 : s.moodIndex
```

后端可以返回 1-5 的评分（会被乘以20转为百分比），或直接返回 0-100 的百分比。

### 7.10 API 基础路径

所有 API 的 baseURL 为 `/api`，因此后端路由需要统一加 `/api` 前缀。例如：
- 前端调用 `/auth/login` → 实际请求 `http://host:port/api/auth/login`
- 前端调用 `/admin/students` → 实际请求 `http://host:port/api/admin/students`

### 7.11 Token 认证

请求拦截器从 localStorage 中读取 token，添加到请求头：
```
Authorization: Bearer <token>
```

后端需要验证此 Header。401 时前端会自动清除登录状态并跳转到登录页。

---

## 八、前端对接状态说明

| 模块 | API 对接状态 | 说明 |
|------|-------------|------|
| 认证 (auth) | 已对接 | Login.vue 和 Register.vue 已实际调用 authApi |
| 学生端 (student) | 未对接 | 所有页面使用 Pinia store mock 数据，API 函数已定义但未调用 |
| 教师端 (teacher) | 未对接 | 所有页面使用硬编码 mock 数据，API 函数已定义但未调用 |
| 管理员端 (admin) | 部分对接 | StudentManagement、TeacherManagement、DailyForum、SubjectForum 已实际调用 adminApi；Home 和 PointOverview 使用 mock 数据 |

> 后端开发完成后，前端需要逐步将 mock 数据替换为实际 API 调用。优先对接已定义 API 函数的页面。
